version: '3.9'

services:
  api:
    build: .
    ports:
      - "5001:5001"
    volumes:
      - .:/app
    env_file:
      - .env
    networks:
      - network1

networks:
  network1:
    name: streaming_network
    external: true